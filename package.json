{
  "name": "auth-yelbolt",
  "version": "1.0.0",
  "private": true,
  "description": "A monorepo for auth-yelbolt and its related projects.",
  "author": "Aurélien Grimaud",
  "license": "MIT",
  "workspaces": [
    "ui-color-palette",
    "ideas-spark-booth",
    "packages/auth-yelbolt-worker"
  ],
  "scripts": {
    "uicp": "npm run dev:uicp& npm run dev:wrk",
    "dev:uicp": "npm run dev -w ui-color-palette",
    "build:uicp": "npm run build -w ui-color-palette",
    "start:uicp": "npm run start -w ui-color-palette",
    "lint:uicp": "npm run lint -w ui-color-palette",
    "patch:uicp": "npm version patch -w ui-color-palette",
    "minor:uicp": "npm version minor -w ui-color-palette",
    "major:uicp": "npm version major -w ui-color-palette",
    "format:uicp": "npm run format -w ui-color-palette",
    "install:uicp": "npm install -w ui-color-palette",
    "isb": "npm run dev:isb & npm run dev:wrk",
    "dev:isb": "npm run dev -w ideas-spark-booth",
    "build:isb": "npm run build -w ideas-spark-booth",
    "start:isb": "npm run start -w ideas-spark-booth",
    "lint:isb": "npm run lint -w ideas-spark-booth",
    "patch:isb": "npm version patch -w ideas-spark-booth",
    "minor:isb": "npm version minor -w ideas-spark-booth",
    "major:isb": "npm version major -w ideas-spark-booth",
    "format:isb": "npm run format -w ideas-spark-booth",
    "install:isb": "npm install -w ideas-spark-booth",
    "dev:wrk": "npm run start:8787 -w auth-yelbolt-worker",
    "stop": "pkill -f 'node'"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
